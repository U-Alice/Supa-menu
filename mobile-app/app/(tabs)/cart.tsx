import { View } from "@/components/View";
import PlaceMenu from "../(screen)/mainLayout/placeMenu";
import CartContent from "../(screen)/mainLayout/cartContent";

export default function Restaurant() {
  return (
    <View className="w-full h-fit ">
      <CartContent />
    </View>
  );
}
